# ============================================
# Human.online Web 前端环境变量配置
# ============================================
# 使用方法：
# 1. 复制此文件为 .env.local:  cp .env.local.example .env.local
# 2. 修改 .env.local 中的配置值
# 3. .env.local 文件不会被提交到 Git（已在 .gitignore 中）
# ============================================

# ============================================
# 后端 API 地址配置（重要）
# ============================================
# 格式: http://域名:端口 （不带 /api/v1 后缀）
#
# 本地开发:
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
#
# 自定义域名部署（示例）:
# NEXT_PUBLIC_API_BASE_URL=http://humind.life:8000
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com

# ============================================
# 前端 API 基础路径
# ============================================
# 用于 axios 和 fetch 请求的 baseURL
# 使用相对路径时会通过 Next.js rewrite 转发到后端
#
# 本地开发（使用 rewrite）:
NEXT_PUBLIC_API_URL=/api/v1
#
# 本地开发（直接访问后端，不使用 rewrite）:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
